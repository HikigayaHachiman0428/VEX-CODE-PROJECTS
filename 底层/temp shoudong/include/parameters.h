#ifndef PARAMETERS_H_
#define PARAMETERS_H_


#ifdef ID_0_  // 领科B
  const float kGyro = 1800.0 / 1770.0;
  const float mogoBTakeAngle = -49;
  const float mogoFTakeAngle = 54;
  const float RLStackAngle = 15;
  const float RLWaitAngle = 74;
  const float RLScoreMiddleAngle = 127;
  const float RLScoreLowAngle = 170;
  const float IMAX = 25;
  const float LeftTwoForward = 35;
  const float LeftTwoTurn = -67;
  const float LeftTwoBack = -20;
#endif

#ifdef ID_1_  // 闵中
  const float kGyro = 1800.0 / 1800.0;
  const float mogoBTakeAngle = -49;
  const float mogoFTakeAngle = 49;
  const float RLStackAngle = 15;
  const float RLWaitAngle = 75;
  const float RLScoreMiddleAngle = 128;
  const float RLScoreLowAngle = 170;
  const float IMAX = 25;
  const float LeftTwoForward = 35;
  const float LeftTwoTurn = -67;
  const float LeftTwoBack = -20;
#endif

#ifdef ID_2_  // 上实
  const float kGyro = 1800.0 / 1800.0;
  const float mogoBTakeAngle = -49;
  const float mogoFTakeAngle = 52;
  const float RLStackAngle = 15;
  const float RLWaitAngle = 75;
  const float RLScoreMiddleAngle = 128;
  const float RLScoreLowAngle = 170;
  const float IMAX = 40;
  const float LeftTwoForward = 30;
  const float LeftTwoTurn = -64;
  const float LeftTwoBack = -25;
#endif

#ifdef ID_3_  // 领科A
  const float kGyro = 1800.0 / 1800.0;
  const float mogoBTakeAngle = -49;
  const float mogoFTakeAngle = 52;
  const float RLStackAngle = 15;
  const float RLWaitAngle = 75;
  const float RLScoreMiddleAngle = 128;
  const float RLScoreLowAngle = 170;
  const float IMAX = 25;
  const float LeftTwoForward = 35;
  const float LeftTwoTurn = -67;
  const float LeftTwoBack = -20;
#endif

#ifdef ID_8_  // 交二
  const float kGyro = 1800.0 / 1800.0;
  const float mogoBTakeAngle = -49;
  const float mogoFTakeAngle = 51;
  const float RLStackAngle = 15;
  const float RLWaitAngle = 75;
  const float RLScoreMiddleAngle = 128;
  const float RLScoreLowAngle = 170;
  const float IMAX = 30;
  const float LeftTwoForward = 40;
  const float LeftTwoTurn = -60;
  const float LeftTwoBack = -25;
#endif


#ifdef ID_10_  // 交附闵
  const float kGyro = 1800.0 / 1800.0;
  const float mogoBTakeAngle = -49;
  const float mogoFTakeAngle = 54;
  const float RLStackAngle = 15;
  const float RLWaitAngle = 75;
  const float RLScoreMiddleAngle = 128;
  const float RLScoreLowAngle = 170;
  const float IMAX = 25;
  const float LeftTwoForward = 30;
  const float LeftTwoTurn = -67;
  const float LeftTwoBack = -20;
#endif

#ifdef ID_23_  // 上中东
  const float kGyro = 1800.0 / 1800.0;
  const float mogoBTakeAngle = -49;
  const float mogoFTakeAngle = 54;
  const float RLStackAngle = 15;
  const float RLWaitAngle = 75;
  const float RLScoreMiddleAngle = 128;
  const float RLScoreLowAngle = 170;
  const float IMAX = 25;
  const float LeftTwoForward = 35;
  const float LeftTwoTurn = -63;
  const float LeftTwoBack = -23;
#endif

#ifdef ID_77_  // 华二A
  const float kGyro = 1800.0 / 1800.0;
  const float mogoBTakeAngle = -49;
  const float mogoFTakeAngle = 52;
  const float RLStackAngle = 15;
  const float RLWaitAngle = 75;
  const float RLScoreMiddleAngle = 120;
  const float RLScoreLowAngle = 170;
  const float IMAX = 25;
  const float LeftTwoForward = 38;
  const float LeftTwoTurn = -64;
  const float LeftTwoBack = -20;
#endif

#ifdef ID_88_  // 华二B
  const float kGyro = 1800.0 / 1800.0;
  const float mogoBTakeAngle = -51;
  const float mogoFTakeAngle = 53;
  const float RLStackAngle = 15;
  const float RLWaitAngle = 75;
  const float RLScoreMiddleAngle = 128;
  const float RLScoreLowAngle = 170;
  const float IMAX = 25;
  const float LeftTwoForward = 35;
  const float LeftTwoTurn = -60;
  const float LeftTwoBack = -30;
#endif

#ifdef ID_99_  // 华二C
  const float kGyro = 1800.0 / 1800.0;
  const float mogoBTakeAngle = -49;
  const float mogoFTakeAngle = 52;
  const float RLStackAngle = 15;
  const float RLWaitAngle = 75;
  const float RLScoreMiddleAngle = 128;
  const float RLScoreLowAngle = 170;
  const float IMAX = 35;
  const float LeftTwoForward = 35;
  const float LeftTwoTurn = -60;
  const float LeftTwoBack = -20;
#endif

#ifdef ID_610_  // 交二
  const float kGyro = 1800.0 / 1800.0;
  const float mogoBTakeAngle = -49;
  const float mogoFTakeAngle = 54;
  const float RLStackAngle = 15;
  const float RLWaitAngle = 75;
  const float RLScoreMiddleAngle = 128;
  const float RLScoreLowAngle = 170;
  const float IMAX = 35;
  const float LeftTwoForward = 35;
  const float LeftTwoTurn = -63;
  const float LeftTwoBack = -20;
#endif

#endif